import React from 'react';
import {Text, View} from 'react-native';
import {text} from '../../styles/app';

function Nfts() {
  return (
    <View>
      <Text style={[text.title, text.color.black]}>NFTs</Text>
    </View>
  );
}

export default Nfts;
